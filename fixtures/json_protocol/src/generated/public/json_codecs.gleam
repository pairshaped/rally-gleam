////
//// Typed JSON encoders and decoders for all discovered types.

// Generated by libero. DO NOT EDIT.

import gleam/dynamic.{type Dynamic}
import gleam/int
import gleam/json
import gleam/option.{type Option, None, Some}
import gleam/result
import libero/json/error.{type JsonError, JsonError}
import libero/json/runtime as json_runtime
import public/pages/home_
import public/pages/notifications_

pub fn json_encode_public_pages_home___server_increment(value) -> json.Json {
  case value {
    home_.ServerIncrement ->
      json.object([
        #("type", json.string("public/pages/home_.ServerIncrement")),
        #("variant", json.string("ServerIncrement")),
        #("fields", json.object([])),
      ])
  }
}

pub fn json_encode_public_pages_home___increment_result(value) -> json.Json {
  case value {
    home_.IncrementResult(f0, f1) ->
      json.object([
        #("type", json.string("public/pages/home_.IncrementResult")),
        #("variant", json.string("IncrementResult")),
        #(
          "fields",
          json.object([
            #(
              "old",
              json.int(
                case
                  f0 >= -9_007_199_254_740_991 && f0 <= 9_007_199_254_740_991
                {
                  True -> f0
                  False -> panic as "Int outside JavaScript safe integer range"
                },
              ),
            ),
            #(
              "new",
              json.int(
                case
                  f1 >= -9_007_199_254_740_991 && f1 <= 9_007_199_254_740_991
                {
                  True -> f1
                  False -> panic as "Int outside JavaScript safe integer range"
                },
              ),
            ),
          ]),
        ),
      ])
  }
}

pub fn json_encode_public_pages_home___server_increment_by(value) -> json.Json {
  case value {
    home_.ServerIncrementBy(f0) ->
      json.object([
        #("type", json.string("public/pages/home_.ServerIncrementBy")),
        #("variant", json.string("ServerIncrementBy")),
        #(
          "fields",
          json.object([
            #(
              "amount",
              json.int(
                case
                  f0 >= -9_007_199_254_740_991 && f0 <= 9_007_199_254_740_991
                {
                  True -> f0
                  False -> panic as "Int outside JavaScript safe integer range"
                },
              ),
            ),
          ]),
        ),
      ])
  }
}

pub fn json_encode_public_pages_home___model(value) -> json.Json {
  case value {
    home_.Model(f0) ->
      json.object([
        #("type", json.string("public/pages/home_.Model")),
        #("variant", json.string("Model")),
        #(
          "fields",
          json.object([
            #(
              "count",
              json.int(
                case
                  f0 >= -9_007_199_254_740_991 && f0 <= 9_007_199_254_740_991
                {
                  True -> f0
                  False -> panic as "Int outside JavaScript safe integer range"
                },
              ),
            ),
          ]),
        ),
      ])
  }
}

pub fn json_encode_public_pages_notifications___model(value) -> json.Json {
  case value {
    notifications_.Model(f0) ->
      json.object([
        #("type", json.string("public/pages/notifications_.Model")),
        #("variant", json.string("Model")),
        #(
          "fields",
          json.object([
            #(
              "count",
              json.int(
                case
                  f0 >= -9_007_199_254_740_991 && f0 <= 9_007_199_254_740_991
                {
                  True -> f0
                  False -> panic as "Int outside JavaScript safe integer range"
                },
              ),
            ),
          ]),
        ),
      ])
  }
}

pub fn json_encode_public_pages_home___to_client(value) -> json.Json {
  case value {
    home_.Updated(f0) ->
      json.object([
        #("type", json.string("public/pages/home_.ToClient")),
        #("variant", json.string("Updated")),
        #(
          "fields",
          json.object([
            #(
              "count",
              json.int(
                case
                  f0 >= -9_007_199_254_740_991 && f0 <= 9_007_199_254_740_991
                {
                  True -> f0
                  False -> panic as "Int outside JavaScript safe integer range"
                },
              ),
            ),
          ]),
        ),
      ])
  }
}

pub fn json_encode_public_pages_notifications___to_client(value) -> json.Json {
  case value {
    notifications_.Updated(f0) ->
      json.object([
        #("type", json.string("public/pages/notifications_.ToClient")),
        #("variant", json.string("Updated")),
        #("fields", json.object([#("msg", json.string(f0))])),
      ])
  }
}

pub fn json_decode_public_pages_home___server_increment(
  value: dynamic.Dynamic,
) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/home_.ServerIncrement" -> Ok(s)
    Ok(s) ->
      Error([
        JsonError(
          "type",
          "expected public/pages/home_.ServerIncrement, got " <> s,
        ),
      ])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("ServerIncrement") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            0 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 0 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      Ok(home_.ServerIncrement)
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_decode_public_pages_home___increment_result(
  value: dynamic.Dynamic,
) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/home_.IncrementResult" -> Ok(s)
    Ok(s) ->
      Error([
        JsonError(
          "type",
          "expected public/pages/home_.IncrementResult, got " <> s,
        ),
      ])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("IncrementResult") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            2 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 2 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      let old_result = case json_runtime.field(fields, "old", "fields.old") {
        Ok(raw) -> json_runtime.int(raw, "fields.old")
        Error(errors) -> Error(errors)
      }
      use old <- result.try(old_result)
      let new_result = case json_runtime.field(fields, "new", "fields.new") {
        Ok(raw) -> json_runtime.int(raw, "fields.new")
        Error(errors) -> Error(errors)
      }
      use new <- result.try(new_result)
      Ok(home_.IncrementResult(old:, new:))
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_decode_public_pages_home___server_increment_by(
  value: dynamic.Dynamic,
) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/home_.ServerIncrementBy" -> Ok(s)
    Ok(s) ->
      Error([
        JsonError(
          "type",
          "expected public/pages/home_.ServerIncrementBy, got " <> s,
        ),
      ])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("ServerIncrementBy") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            1 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 1 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      let amount_result = case
        json_runtime.field(fields, "amount", "fields.amount")
      {
        Ok(raw) -> json_runtime.int(raw, "fields.amount")
        Error(errors) -> Error(errors)
      }
      use amount <- result.try(amount_result)
      Ok(home_.ServerIncrementBy(amount:))
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_decode_public_pages_home___model(value: dynamic.Dynamic) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/home_.Model" -> Ok(s)
    Ok(s) ->
      Error([JsonError("type", "expected public/pages/home_.Model, got " <> s)])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("Model") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            1 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 1 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      let count_result = case
        json_runtime.field(fields, "count", "fields.count")
      {
        Ok(raw) -> json_runtime.int(raw, "fields.count")
        Error(errors) -> Error(errors)
      }
      use count <- result.try(count_result)
      Ok(home_.Model(count:))
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_decode_public_pages_notifications___model(value: dynamic.Dynamic) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/notifications_.Model" -> Ok(s)
    Ok(s) ->
      Error([
        JsonError(
          "type",
          "expected public/pages/notifications_.Model, got " <> s,
        ),
      ])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("Model") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            1 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 1 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      let count_result = case
        json_runtime.field(fields, "count", "fields.count")
      {
        Ok(raw) -> json_runtime.int(raw, "fields.count")
        Error(errors) -> Error(errors)
      }
      use count <- result.try(count_result)
      Ok(notifications_.Model(count:))
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_decode_public_pages_home___to_client(value: dynamic.Dynamic) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/home_.ToClient" -> Ok(s)
    Ok(s) ->
      Error([
        JsonError("type", "expected public/pages/home_.ToClient, got " <> s),
      ])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("Updated") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            1 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 1 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      let count_result = case
        json_runtime.field(fields, "count", "fields.count")
      {
        Ok(raw) -> json_runtime.int(raw, "fields.count")
        Error(errors) -> Error(errors)
      }
      use count <- result.try(count_result)
      Ok(home_.Updated(count:))
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_decode_public_pages_notifications___to_client(
  value: dynamic.Dynamic,
) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok(s) if s == "public/pages/notifications_.ToClient" -> Ok(s)
    Ok(s) ->
      Error([
        JsonError(
          "type",
          "expected public/pages/notifications_.ToClient, got " <> s,
        ),
      ])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("Updated") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use _ <- result.try(case json_runtime.object_size(fields, "fields") {
        Ok(size) ->
          case size {
            1 -> Ok(Nil)
            n ->
              Error([
                JsonError(
                  "fields",
                  "expected exactly 1 object fields, got " <> int.to_string(n),
                ),
              ])
          }
        Error(errors) -> Error(errors)
      })
      let msg_result = case json_runtime.field(fields, "msg", "fields.msg") {
        Ok(raw) -> json_runtime.string(raw, "fields.msg")
        Error(errors) -> Error(errors)
      }
      use msg <- result.try(msg_result)
      Ok(notifications_.Updated(msg:))
    }
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
    Ok(s) -> Error([JsonError("variant", "unknown: " <> s)])
  }
}

pub fn json_encode_gleam_option__option(
  value,
  encode_inner: fn(a) -> json.Json,
) -> json.Json {
  case value {
    Some(inner) ->
      json.object([
        #("type", json.string("gleam/option.Option")),
        #("variant", json.string("Some")),
        #("fields", json.array([encode_inner(inner)], of: fn(x) { x })),
      ])
    None ->
      json.object([
        #("type", json.string("gleam/option.Option")),
        #("variant", json.string("None")),
        #("fields", json.object([])),
      ])
  }
}

pub fn json_encode_gleam_result__result(
  value,
  encode_ok: fn(a) -> json.Json,
  encode_err: fn(b) -> json.Json,
) -> json.Json {
  case value {
    Ok(inner) ->
      json.object([
        #("type", json.string("gleam/result.Result")),
        #("variant", json.string("Ok")),
        #("fields", json.array([encode_ok(inner)], of: fn(x) { x })),
      ])
    Error(inner) ->
      json.object([
        #("type", json.string("gleam/result.Result")),
        #("variant", json.string("Error")),
        #("fields", json.array([encode_err(inner)], of: fn(x) { x })),
      ])
  }
}

pub fn json_decode_gleam_option__option(
  value: Dynamic,
  decode_inner: fn(Dynamic) -> Result(a, List(JsonError)),
) -> Result(Option(a), List(JsonError)) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok("gleam/option.Option") -> Ok(Nil)
    Ok(s) ->
      Error([JsonError("type", "expected gleam/option.Option, got " <> s)])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("None") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use size <- result.try(json_runtime.object_size(fields, "fields"))
      case size {
        0 -> Ok(None)
        n ->
          Error([
            JsonError(
              "fields",
              "expected exactly 0 object fields, got " <> int.to_string(n),
            ),
          ])
      }
    }
    Ok("Some") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use size <- result.try(json_runtime.array_length(fields, "fields"))
      use _ <- result.try(case size {
        1 -> Ok(Nil)
        n ->
          Error([
            JsonError("fields", "expected 1 elements, got " <> int.to_string(n)),
          ])
      })
      use inner_raw <- result.try(json_runtime.array_at(fields, 0, "fields[0]"))
      use val <- result.try(decode_inner(inner_raw))
      Ok(Some(val))
    }
    Ok(other) ->
      Error([JsonError("variant", "unknown Option variant: " <> other)])
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
  }
}

pub fn json_decode_gleam_result__result(
  value: Dynamic,
  decode_ok: fn(Dynamic) -> Result(a, List(JsonError)),
  decode_err: fn(Dynamic) -> Result(b, List(JsonError)),
) -> Result(Result(a, b), List(JsonError)) {
  use _ <- result.try(case json_runtime.field_string(value, "type", "type") {
    Ok("gleam/result.Result") -> Ok(Nil)
    Ok(s) ->
      Error([JsonError("type", "expected gleam/result.Result, got " <> s)])
    Error(_) -> Error([JsonError("type", "missing or not a string")])
  })
  case json_runtime.field_string(value, "variant", "variant") {
    Ok("Ok") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use size <- result.try(json_runtime.array_length(fields, "fields"))
      use _ <- result.try(case size {
        1 -> Ok(Nil)
        n ->
          Error([
            JsonError("fields", "expected 1 elements, got " <> int.to_string(n)),
          ])
      })
      use inner_raw <- result.try(json_runtime.array_at(fields, 0, "fields[0]"))
      use val <- result.try(decode_ok(inner_raw))
      Ok(Ok(val))
    }
    Ok("Error") -> {
      use fields <- result.try(json_runtime.field(value, "fields", "fields"))
      use size <- result.try(json_runtime.array_length(fields, "fields"))
      use _ <- result.try(case size {
        1 -> Ok(Nil)
        n ->
          Error([
            JsonError("fields", "expected 1 elements, got " <> int.to_string(n)),
          ])
      })
      use inner_raw <- result.try(json_runtime.array_at(fields, 0, "fields[0]"))
      use val <- result.try(decode_err(inner_raw))
      Ok(Error(val))
    }
    Ok(other) ->
      Error([JsonError("variant", "unknown Result variant: " <> other)])
    Error(_) -> Error([JsonError("variant", "missing or not a string")])
  }
}
